prototype(Plyr:Notification) < prototype(Carbon.Notification:Backend) {
  key = null
  @if.render = ${this.key}
  type = 'alert'
  content = ${Translation.translate('backendAlert.' + this.key, null, [], null, 'Jonnitto.Plyr') }
}
